apiVersion: v1
kind: Service
metadata:
  name: {{ include "zilobase.fullname" . }}
  labels:
    {{- include "zilobase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "zilobase.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
