{
  "name": "@zilobase/desktop",
  "version": "0.0.55",
  "license": "MIT",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env.development --run tauri -- dev",
    "build": "npm run tokens:check --prefix ../.. && node --env-file-if-exists=../../.env.development --run tauri -- build",
    "test:selfhost": "node e2e/selfhost.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
