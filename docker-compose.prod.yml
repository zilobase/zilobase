services:
  zilobase:
    # The base file already contains the production topology. This small
    # overlay makes production pulls explicit for deployment automation.
    image: ${ZILOBASE_IMAGE:?Set ZILOBASE_IMAGE to an immutable release tag or digest}
    pull_policy: always
