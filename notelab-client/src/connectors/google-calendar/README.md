# @notelab/google-calendar-connector

Read-only Google Calendar connector primitives for Notelab packages.

The base connector requests the user's own Calendar read-only access. Coworker
availability is opt-in and uses the Google Calendar free/busy scope.
